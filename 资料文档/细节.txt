foreach 传入的是值（像C中的参数）
类在创建新对象或引用静态成员或使用类加载器加载时载入
gc管理的是通过new在堆中创建的对象，通过其他方式获取的内存资源（如通过本地方法获取的内存资源）需要手动释放
java中的gc管理的是内存资源（堆上）
构造器实际上是static方法（该static是隐式的） private方法实际上是final的（该final是隐式的）
构造器可以调用导出类里的方法，因而在构造其中应避免调用其他方法，除非调用的是基类中的final方法